# Copiar a `server/.env` y rellenar con creds reales (https://developer.spotify.com/dashboard).
# `.env` está gitignoreado a nivel del repo — nunca commitees creds reales.

SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
